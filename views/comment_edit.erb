<%= erb :partial_news, :locals => { :news => @news } %>
<%= erb :partial_comment, :locals => { :c => @comment, :u => @user } %>
<%= erb :partial_comment_form, :locals => { :news_id => @news["id"], :comment_id => params["comment_id"], :body => @comment['body'], :parent_id => -1 } %>
<note>Note: to remove the comment remove all the text and press Edit.</note>
